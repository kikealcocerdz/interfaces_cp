---
const { color, title, subtitle, id } = Astro.props;
const textColor = `text-${color}`;
let imageSrc = `/tesla_${color}.jpeg`;

function changeColor(color) {
  imageSrc = `/tesla_${color}.jpeg`;
}
---

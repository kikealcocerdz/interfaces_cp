---
const { step } = Astro.props
---

<div>
  <nav class="h-full flex flex-row w-full mt-10 py-8">
    <div class=`flex flex-col items-center justify-evenly w-1/3 mt-5 px-10`><a href="#section-lugar"   class={`border-2 border-black rounded-full w-16 h-16  ${step[0] === 1 ? 'bg-black' : 'bg-white'}`}></a>Lugar</div>
    <div class=`flex flex-col items-center justify-evenly w-1/3 mt-5 px-10`><a href="#section-datos"  class={`border-2 border-black rounded-full w-16 h-16  ${step[1] === 2 ? 'bg-black' : 'bg-white'}`}></a>Datos</div>
    <div class=`flex flex-col items-center justify-evenly w-1/3 mt-5 px-10`><a href="#section-confirmar"class={`border-2 border-black rounded-full w-16 h-16  ${step[2] === 3 ? 'bg-black' : 'bg-white'}`}></a>Confirmación</div>
  </nav>
</div>
